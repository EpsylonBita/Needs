# Auth Components

This directory contains components specific to the auth feature.

## Organization

- Place all auth-specific components in this directory
- Use kebab-case for file names
- Export components using named exports
- Group related components in subdirectories if needed
- All files must use TypeScript (.ts or .tsx)

## Relationship to App Router

These components are used in the `/app/auth/` route.
